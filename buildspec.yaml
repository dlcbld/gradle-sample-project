version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash
env:
  variables:
    "testEnv" : "defaultTestVariable"
  exportedVariables:
    - VERS
    - param1

steps:
  - type: Command
    name: "pre-build"
    command: |
      echo "build is starting"
  - type: Command
    name: "public internet access build"
    command: |
      pip install -r requirements.txt
