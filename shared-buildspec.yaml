version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash
env:
  variables:
    "testEnv" : "defaultTestVariable"
  exportedVariables:
    - VERS
    - param1

steps:
  - type: Command
    name: "buildSource1"
    command: |
      cd ${OCI_WORKSPACE_DIR}/TestBuildSource1
      mvn clean install
      java -jar target/my-app-1.0-SNAPSHOT.jar
      mv target/my-app-1.0-SNAPSHOT.jar buildSource1.jar
  - type: Command
    name: "buildSource2"
    command: |
      cd ${OCI_WORKSPACE_DIR}/TestBuildSource2
      ./gradlew build
outputArtifacts:
- name: maven
  type: BINARY
  location: ${OCI_WORKSPACE_DIR}/TestBuildSource1/target/my-app-1.0-SNAPSHOT.jar
- name: gradle
  type: BINARY
  location: ${OCI_WORKSPACE_DIR}/TestBuildSource2/build/libs/gradle-build-scan-quickstart.jar
